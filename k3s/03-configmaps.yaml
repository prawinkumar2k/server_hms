apiVersion: v1
kind: ConfigMap
metadata:
  name: hms-backend-config
  namespace: hms-production
data:
  PORT: "3000"
  DB_HOST: "db"
  DB_USER: "root"
  DB_NAME: "hms_db"
  NODE_ENV: "production"
  LOG_TO_CONSOLE: "true"
