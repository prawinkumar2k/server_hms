You are a senior authentication and security engineer.

TASK:
Completely REMOVE the existing login/authentication logic from the HMS project and REBUILD a real, production-grade Role-Based Authentication (RBAC) system from scratch.

SYSTEM OVERVIEW:
This HMS has exactly 5 roles (modules):
1. Admin
2. Doctor
3. Receptionist
4. Lab Technician
5. Pharmacist

CORE RULES (NON-NEGOTIABLE):
- There must be ONLY ONE authentication system (no per-user or per-role auth scripts).
- Credentials created by Admin MUST remain valid until explicitly changed or deactivated.
- No hardcoded users.
- No mock or temporary authentication.
- No role-based script duplication.

ADMIN RULES:
- Admin is the ONLY role that can:
  - Create users
  - Assign roles
  - Reset passwords
  - Activate / deactivate users
- Admin can create:
  - Other Admins (with same permissions)
  - Doctors
  - Receptionists
  - Lab Technicians
  - Pharmacists
- Admin-generated UserID and Password are shared securely with the respective user.

AUTHENTICATION FLOW:
1. User enters UserID + Password on a single Login Page
2. System validates credentials against the database
3. System fetches role from database
4. System generates auth token/session
5. User is redirected ONLY to their role-specific dashboard
6. Unauthorized route access must be blocked at both client and server side

AUTHORIZATION (RBAC):
- Admin → Full system access
- Doctor → Medical + patient-related modules only
- Receptionist → Patient registration + appointment modules only
- Lab Technician → Lab orders + lab reports only
- Pharmacist → Pharmacy + medicine inventory only
- Role permissions must be enforced server-side (not UI-only)

TECHNICAL REQUIREMENTS:
- Single users table with role field
- Passwords must be securely hashed
- Auth must be token/session based (not local-only flags)
- Middleware/guards must validate:
  - Authentication
  - Authorization (role)
- Login page and Admin dashboard must be created (they currently do not exist)
- Existing role dashboards (Doctor, Receptionist, Lab, Pharmacy) must remain unchanged

DELIVERABLES:
1. Clean authentication architecture explanation
2. Database schema for users and roles
3. Admin user management flow
4. Login flow (end-to-end)
5. Backend auth middleware logic
6. Frontend route protection logic
7. Clear separation between authentication and authorization

IMPORTANT:
- Do NOT create separate auth logic per role.
- Do NOT auto-generate users.
- Do NOT invalidate credentials unless Admin changes them.
- Think like a real hospital system used in production.

Proceed step-by-step and rebuild the system cleanly.
